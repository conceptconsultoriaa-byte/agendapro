/* Copie este arquivo para "config.js" (mesma pasta) e cole as chaves do seu projeto Supabase.
   Onde achar: no painel do Supabase → Project Settings → API.
   A "anon key" é pública por design (protegida pelas regras de RLS do banco) —
   pode ficar exposta no navegador sem problema. */
window.SUPABASE_URL = "https://SEU-PROJETO.supabase.co";
window.SUPABASE_ANON_KEY = "SUA-CHAVE-ANON-PUBLICA";
